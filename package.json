{
  "name": "crypt-json",
  "version": "1.0.1",
  "description": "Javascript Class (and scripts) to crypt/decrypt some json fields",
  "main": "CryptJson.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["LUCipher", "encrypt", "decrypt", "json"],
  "author": "manufosela",
  "license": "Apache-2.0",
  "dependencies": {
    "lucipher": "^1.0.0"
  }
}
